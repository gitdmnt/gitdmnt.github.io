---

---

<header
  class="md:fixed md:h-dvh w-full md:w-28 md:self-start md:flex md:items-center md:justify-center
  px-12 py-8 md:p-0 md:border-r md:border-black md:border-dashed"
>
  <div
    class="w-full h-full flex flex-row items-center justify-between text-nowrap"
  >
    <a class="text-2xl md:text-xl black md:-rotate-90" href="/"
      >や<span>-</span>さがされ</a
    >
    <ul class="listnone flex flex-row gap-2 md:hidden">
      <li class="border-b border-black border-dashed">
        <a class="black" href="/about">げたばこ</a>
      </li>
      <li class="border-b border-black border-dashed">
        <a class="black" href="/blog/1">やねうら</a>
      </li>
    </ul>
  </div>
</header>
<style>
  ul {
    margin: 0;
    padding: 0;
  }
  li {
    margin: 0;
    padding: 0;
  }
  .black {
    color: #1d1d1d;
  }
  .border-black {
    border-color: #1d1d1d;
  }
  .listnone {
    list-style: none;
  }
  .writing-vertical {
    transform: rotate(-90deg);
  }
</style>
<!-- 
<style>
  header {
    display: grid;
    place-items: center center;

    width: 100%;
    height: 100dvh;

    position: sticky;
    top: 0;

    border-inline-end: 1px dashed #1d1d1d;

    font-family: "Noto Sans CJK JP";
    font-weight: 300;
  }
  .title {
    writing-mode: sideways-lr;
  }
  .link-container {
    display: none;
  }

  a {
    color: #1d1d1d;
    text-decoration: none;
  }
  @container (max-width: 768px) {
    header {
      grid-template-columns: auto 1fr;
      justify-items: stretch;
      justify-content: start;

      width: 100%;
      height: auto;

      border-inline-end: none;

      margin-block-end: 4rem;
      padding-block-end: 4rem;
    }
    .title {
      margin-block: 2rem;
      margin-inline: 1rem;
      writing-mode: horizontal-tb;
      font-size: 12pt;
      white-space: nowrap;
    }
    div.title:first-letter {
      font-size: 20pt;
    }
    span.title {
      display: none;
    }
    .link-container {
      justify-self: end;
      display: flex;
      font-size: 10pt;
      margin-block-start: calc(2rem + 9pt);
      margin-block-end: calc(2rem + 1pt);
      padding: 0;
    }
    .page-link {
      display: block;
      margin-inline-end: 1rem;
      padding: 0;
      border-block-end: 1px dashed #1d1d1d;
    }
  }
</style>
-->
